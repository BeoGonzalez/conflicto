name: Mi workflow para subir imagen a docker
on:
 push:
  branches: [main]
jobs:
  checkout:
  runs-on: ubuntu latest
  steps:
    - name: Descargar codigo
      uses: actions/checkout@v4

    - name: ejecuta un saludo
      run: echo "Hola mundo"!

    - name: lista de archivos
      run: ls -R